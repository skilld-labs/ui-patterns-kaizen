{%
  set classes = [
    'a-button',
    a_button_modifier|safe_join(' '),
    a_button_extra_classes,
  ]
%}
<{{ tag|default('button') }}{{ attributes.addClass(classes) }}>
  {{ content }}
{#  {% include '@atoms/icon/a-icon.html.twig' %}#}
</{{ tag|default('button') }}>
