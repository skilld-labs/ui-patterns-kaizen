/**
  * DO NOT EDIT THIS FILE.
  * It's generated automatically by 'yarn build' command.
  * @preserve
**/
(({ behaviors }) => {
  behaviors.eshopgen_base_theme_storybook_o_branding = {
    defaultEntry: () => {
      return {
        className: "o-branding",
        processingName: "storybook-o-branding"
      };
    },
    customEntry: () => {
    },
    entries: () => {
      const entries = [
        behaviors.eshopgen_base_theme_storybook_o_branding.defaultEntry()
      ];
      if (behaviors.eshopgen_base_theme_storybook_o_branding.customEntry()) {
        entries.push(
          ...behaviors.eshopgen_base_theme_storybook_o_branding.customEntry()
        );
      }
      return entries;
    },
    attach: (context) => {
      behaviors.eshopgen_base_theme_storybook_o_branding.entries().forEach((entry) => {
        once(entry.processingName, `.${entry.className}`, context).forEach(
          (el) => {
            behaviors.eshopgen_base_theme_storybook_o_branding.handler({
              el,
              entry
            });
          }
        );
      });
    },
    handler: (obj) => {
      console.log(obj.el);
    }
  };
})(Drupal);
