/**
  * DO NOT EDIT THIS FILE.
  * It's generated automatically by 'yarn build' command.
  * @preserve
**/
(({ behaviors }) => {
  behaviors.eshopgen_base_theme_storybook_a_checkbox = {
    defaultEntry: () => {
      return {
        className: "a-checkbox",
        processingName: "storybook-a-checkbox"
      };
    },
    customEntry: () => {
    },
    entries: () => {
      const entries = [
        behaviors.eshopgen_base_theme_storybook_a_checkbox.defaultEntry()
      ];
      if (behaviors.eshopgen_base_theme_storybook_a_checkbox.customEntry()) {
        entries.push(
          ...behaviors.eshopgen_base_theme_storybook_a_checkbox.customEntry()
        );
      }
      return entries;
    },
    attach: (context) => {
      behaviors.eshopgen_base_theme_storybook_a_checkbox.entries().forEach((entry) => {
        once(entry.processingName, `.${entry.className}`, context).forEach(
          (el) => {
            behaviors.eshopgen_base_theme_storybook_a_checkbox.handler({
              el,
              entry
            });
          }
        );
      });
    },
    handler: (obj) => {
      console.log(obj.el);
    }
  };
})(Drupal);
