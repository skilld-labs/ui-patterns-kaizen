/**
  * DO NOT EDIT THIS FILE.
  * It's generated automatically by 'yarn build' command.
  * @preserve
**/
(({ behaviors }) => {
  behaviors.eshopgen_base_theme_storybook_a_throbber = {
    defaultEntry: () => {
      return {
        className: "a-throbber",
        processingName: "storybook-a-throbber"
      };
    },
    customEntry: () => {
    },
    entries: () => {
      const entries = [
        behaviors.eshopgen_base_theme_storybook_a_throbber.defaultEntry()
      ];
      if (behaviors.eshopgen_base_theme_storybook_a_throbber.customEntry()) {
        entries.push(
          ...behaviors.eshopgen_base_theme_storybook_a_throbber.customEntry()
        );
      }
      return entries;
    },
    attach: (context) => {
      behaviors.eshopgen_base_theme_storybook_a_throbber.entries().forEach((entry) => {
        once(entry.processingName, `.${entry.className}`, context).forEach(
          (el) => {
            behaviors.eshopgen_base_theme_storybook_a_throbber.handler({
              el,
              entry
            });
          }
        );
      });
    },
    handler: (obj) => {
      console.log(obj.el);
    }
  };
})(Drupal);
