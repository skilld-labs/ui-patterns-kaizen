/**
  * DO NOT EDIT THIS FILE.
  * It's generated automatically by 'yarn build' command.
  * @preserve
**/
(({ behaviors }) => {
  behaviors.eshopgen_base_theme_storybook_m_form_element_label = {
    defaultEntry: () => {
      return {
        className: "m-form-element-label",
        processingName: "storybook-m-form-element-label"
      };
    },
    customEntry: () => {
    },
    entries: () => {
      const entries = [
        behaviors.eshopgen_base_theme_storybook_m_form_element_label.defaultEntry()
      ];
      if (behaviors.eshopgen_base_theme_storybook_m_form_element_label.customEntry()) {
        entries.push(
          ...behaviors.eshopgen_base_theme_storybook_m_form_element_label.customEntry()
        );
      }
      return entries;
    },
    attach: (context) => {
      behaviors.eshopgen_base_theme_storybook_m_form_element_label.entries().forEach((entry) => {
        once(entry.processingName, `.${entry.className}`, context).forEach(
          (el) => {
            behaviors.eshopgen_base_theme_storybook_m_form_element_label.handler(
              { el, entry }
            );
          }
        );
      });
    },
    handler: (obj) => {
      console.log(obj.el);
    }
  };
})(Drupal);
