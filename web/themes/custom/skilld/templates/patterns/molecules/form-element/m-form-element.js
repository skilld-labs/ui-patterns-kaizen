/**
  * DO NOT EDIT THIS FILE.
  * It's generated automatically by 'yarn build' command.
  * @preserve
**/
(({ behaviors }) => {
  behaviors.eshopgen_base_theme_storybook_m_form_element = {
    defaultEntry: () => {
      return {
        className: "m-form-element",
        processingName: "storybook-m-form-element"
      };
    },
    customEntry: () => {
    },
    entries: () => {
      const entries = [
        behaviors.eshopgen_base_theme_storybook_m_form_element.defaultEntry()
      ];
      if (behaviors.eshopgen_base_theme_storybook_m_form_element.customEntry()) {
        entries.push(
          ...behaviors.eshopgen_base_theme_storybook_m_form_element.customEntry()
        );
      }
      return entries;
    },
    attach: (context) => {
      behaviors.eshopgen_base_theme_storybook_m_form_element.entries().forEach((entry) => {
        once(entry.processingName, `.${entry.className}`, context).forEach(
          (el) => {
            behaviors.eshopgen_base_theme_storybook_m_form_element.handler({
              el,
              entry
            });
          }
        );
      });
    },
    handler: (obj) => {
      console.log(obj.el);
    }
  };
})(Drupal);
