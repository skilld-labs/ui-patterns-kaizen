{% set classes = [
  'a-icon',
  extra_classes ? extra_classes : [],
] %}
<svg {{ attributes.addClass(classes).setAttribute('aria-hidden', 'true') }}>
  <use xlink:href="{{ kaizer_svg_sprite }}#svg-{{ icon }}"></use>
</svg>
